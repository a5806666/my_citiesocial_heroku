<%= form_with(model: [:admin, vendor]) do |form| %>
  <div class="field">
    <%= form.label :title, '名前', class: 'label' %>
    <%= form.text_field :title, class: 'input' %>
  </div>

  <div class="field">
    <%= form.label :description, '説明', class: 'label' %>
    <%= form.text_area :description, class: 'textarea' %>
  </div>

  <div class="field">
    <%= form.check_box :online %>
    <%= form.label :online, 'サービス中？', class: 'checkbox' %>
  </div>

  <%= form.submit class: 'button is-dark is-medium' %>
<% end %>